import { HeaderContainer } from "../style/Header.styled";

const Header = () => {
  return (
    <HeaderContainer>
      <h2>{`</aTe>`}</h2>
      <h1>Firecontact App</h1>
    </HeaderContainer>
  );
};

export default Header;
